# Typescript Sorting

## Config

    $ tsc --init
    $ vim tsconfig.json
    // "outDir": "./build",
    // "rootDir": "./src",

## Run

    $ tsc

    // watch and recompile automatically
    $ tsc -W
